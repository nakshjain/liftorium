spring.application.name=gym-helper-backend
spring.profiles.active=development

server.port=${PORT:4000}
spring.mongodb.uri=${MONGODB_URI}

app.cors.allowed-origin=${CORS_ORIGIN:http://localhost:4200}
app.jwt.access-secret=${JWT_ACCESS_SECRET}
app.jwt.refresh-secret=${JWT_REFRESH_SECRET}
app.jwt.access-token-ttl=${ACCESS_TOKEN_TTL:15m}
app.jwt.refresh-token-ttl=${REFRESH_TOKEN_TTL:30d}
app.jwt.refresh-token-cookie-name=${REFRESH_TOKEN_COOKIE_NAME:gym_refresh_token}
app.jwt.refresh-token-cookie-path=/api/v1/auth
app.ascend-api.base-url=${ASCEND_API_BASE_URL:https://edb-with-videos-and-images-by-ascendapi.p.rapidapi.com}
app.ascend-api.host=${ASCEND_API_HOST:edb-with-videos-and-images-by-ascendapi.p.rapidapi.com}
app.ascend-api.api-key=${ASCEND_API_KEY}
app.exercises.sync-on-startup=${EXERCISE_SYNC_ON_STARTUP:false}
